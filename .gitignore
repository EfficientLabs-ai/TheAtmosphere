node_modules/
.env
.env.*
*.key
*.pem
*.pat
.secrets-vault/
.DS_Store

# operator's mesh-node trust anchor (pinned origin key) — keep private. config.example.json stays tracked.
node-runner/config.json
